pipeline{
    agent any
    tools{
        maven 'Maven'
    }    
    stages{
        stage('git checkout')
        {
            steps{
                git credentialsId: 'github', url: 'https://github.com/siddireddylakshman/devops-automation.git'
            }
        }
        stage('Maven Build'){
            steps{
                bat 'mvn clean pacakge'
            }
        }        
    }
}
